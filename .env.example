# URL do servidor de sincronização (Yjs via WebSocket).
# Dev: o servidor local sobe junto com `npm run dev` (porta 1234).
# Produção: wss://sync.SEU-DOMINIO.com — Hocuspocus na VPS Oracle, atrás do Cloudflare.
VITE_SYNC_URL=ws://localhost:1234
